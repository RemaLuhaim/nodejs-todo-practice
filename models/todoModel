
const mongoose = require('mongoose');

const Schema = mongoose.Schema({ //empty schema
    title: String,
    description: String,
    date: Date
});

const Task = mongoose.model('Task', Schema);

module.exports = Task; //exporting the model to be used in the ToDoTask.js file